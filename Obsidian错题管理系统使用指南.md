# Obsidian 错题管理系统 — 使用指南

## 系统简介

以**截图**为核心的错题管理方案。每道错题是一张截图 + 标签（考点、难度、掌握程度、正确答案），通过 Dataview 筛选标签后随机抽取组成试卷，截图直接展示在卷面上。

**核心流程：** 截图错题 → 快捷键录入 + 打标签 → 看仪表盘定位薄弱点 → 随机组卷自测 → 标记掌握程度（自动安排下次复习）

---

## 一、准备工作

### 1.1 安装 Obsidian

官网下载：https://obsidian.md

### 1.2 打开 Vault

将本项目文件夹（包含 `.obsidian`）拖入 Obsidian 的「打开文件夹作为 Vault」。`.obsidian` 内含全部插件和配置，打开即用。

### 1.3 确认插件已启用

设置 → 第三方插件 → 确认以下三个为**绿色开关**：

| 插件 | 作用 |
|---|---|
| **Dataview** | 数据库查询引擎 |
| **Templater** | 模板系统 |
| **QuickAdd** | 一键录入快捷键 |

> 已预装好，无需额外安装。如开关为灰色，点击开启即可。

### 1.4 查看已有快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+Shift+E` | 录入新错题 |
| `Ctrl+Shift+M` | 更新掌握程度（复习次数自动 +1） |

> 如需修改：设置 → 快捷键 → 搜索对应命令 → 重新绑定。

---

## 二、日常使用

### 2.1 录入错题

1. 手机拍照或截屏错题
2. 在 Obsidian 中按 `Ctrl+Shift+E`
3. 依次弹出输入框：
   - **题号**（作为文件名，如「第18题」「P15-3」）
   - **考点**：弹出已有考点列表，直接打字即可过滤，选中后回车；一个题有多个考点时选「✅ 完成」前可继续添加
   - **错因**：同上，从已有错因中选择，也可继续添加多个
   - **难度**（下拉选：简单 / 中等 / 困难）
   - **正确答案**（可留空；组卷时默认折叠显示，点「查看答案」展开）
4. 自动生成新笔记，将截图 `Ctrl+V` 粘贴进去
5. 保存

> 笔记顶部 frontmatter 已自动填好标签，无需手动编辑。笔记底部有结构化「我的复盘」区（错在哪 / 正确思路 / 下次注意），复习后填写。截图粘贴后会自动使用完整路径引用，不会因同名图片混淆。在「选择考点 / 错因 / 难度」环节按 ESC 会取消录入（生成一条「录入已取消」的笔记，删掉即可）；在「还要继续添加吗」环节按 ESC 会保留已选内容继续。

> 💡 **规范词表**：列表里的词来自 `脚本/规范词表.json`，录入时选过的词会自动保存进去，下次直接出现在列表里；没有合适的词时选择「✨ 新建考点 / 新建错因」，新词也会自动入库。这样同一个考点永远只会有一种写法，仪表盘和组卷不会越用越乱。

### 2.2 查看统计

打开 `错题仪表盘.md`，切到**阅读模式**（`Ctrl+E`），可以看到：

- **今日待复习**（分「已逾期」和「今天到期」两组，逾期的优先复习，最优先看这个）
- **掌握程度分布**（未掌握 / 部分掌握 / 已掌握，彩色柱状条）
- **难度分布**
- **薄弱考点 TOP 10**（只统计未掌握 + 部分掌握的题，你当前最需要攻克的考点排行）
- **错因分布**（只统计未掌握 + 部分掌握的题）
- **每周新增趋势**
- **最近复习动态**（每次标记掌握程度的轨迹，看到自己的进步）

### 2.3 随机组卷自测

1. 打开 `试卷/随机组卷.md`
2. 切到**编辑模式**，修改顶部 frontmatter 的筛选条件：

```yaml
difficulty-filter: [中等, 困难]   # 只抽中等和困难的题
mastery-filter: [未掌握]          # 只抽还没掌握的
kp-filter: ""                     # 空 = 不限考点；填"函数"则只抽含该考点的题
reason-filter: ""                 # 空 = 不限错因；填"概念不清"则只抽该错因的题
due-only: true                    # true = 只练到期题（不足不凑数）；false = 到期优先，未到期题补满（加练）
skip-recent-days: 2               # 几天内抽过的不再抽（仅作用于未到期题）；0 = 不限
count: 5                          # 每次抽几道
show-answer: false                # false = 答案屏幕隐藏防剧透（导出 PDF 自动含答案）；true = 直接显示
```

   | 字段 | 可选值 | 说明 |
   |---|---|---|
   | `difficulty-filter` | `简单`、`中等`、`困难` | `[]` = 不限 |
   | `mastery-filter` | `未掌握`、`部分掌握`、`已掌握` | `[]` = 不限 |
   | `kp-filter` | 任意考点名 | `""` = 不限 |
   | `reason-filter` | 任意错因名 | `""` = 不限 |
   | `due-only` | `true` / `false` | `true`=只练到期题，不足 `count` 也不拿未到期题凑数（默认）；`false`=到期题优先，未到期题补满（加练） |
   | `skip-recent-days` | 数字 | 几天内抽过的不再抽（默认 `2`，`0` = 不限）；**仅作用于未到期题**——已到期题永远优先出现、不受防重复限制 |
   | `count` | 数字 | 抽题数量 |
   | `show-answer` | `true` / `false` | `false`=屏幕隐藏防剧透（默认，导出 PDF 自动含答案）/ `true`=直接显示 |

3. 切回**阅读模式**（`Ctrl+E`），截图直接排列在下方
4. 做题过程中，每道题下方有「✅ 会了 / ❌ 不会」按钮，点一下立即更新该题的掌握程度并安排下次复习（详见 2.4）。**本套卷抽定后保持固定，标记不会换题**（刷新、切换笔记都不换），点「✅ 本次已做完」才换新一批
5. 做完后点页面底部的「✅ 本次已做完」按钮换下一批：标记过「会了 / 不会」的题按各自复习间隔（1 → 3 → 7 → 15 → 30 天，见 2.4）到期后再出现；没标记的到期题继续轮换出现（按最久没做优先）；防重复窗口只作用于未到期题（默认 2 天）
6. 可 `Ctrl+P` → 导出 PDF 打印练习

> 组卷时笔记里的截图会全部显示；答案默认隐藏，每题旁有「👀 查看答案」可单独展开；**导出 PDF 时答案会自动包含、交互按钮自动隐藏**，无需任何额外操作。想屏幕上直接显示把 `show-answer` 改为 `true`。

### 2.4 标记掌握程度

两种方式：

**方式一：组卷自测时直接标记**（推荐）
做卷子时，每道题下方有「✅ 会了 / ❌ 不会」按钮，点一下立即更新掌握程度并安排下次复习，无需打开笔记。

**方式二：单独复习时标记**
1. 打开该题笔记
2. 按 `Ctrl+Shift+M`（或 `Ctrl+P` 运行「QuickAdd: 更新掌握程度」）
3. 选择新的掌握程度（未掌握 / 部分掌握 / 已掌握）

两种方式都会：复习次数 +1、写入一条复习轨迹（`review-log`，见仪表盘「最近复习动态」）、自动安排下次复习日期：

| 标记结果 | 下次复习间隔 |
|---|---|
| 未掌握（或点「❌ 不会」） | 1 天 |
| 部分掌握 | 3 → 7 → 15 → 30 天递进 |
| 已掌握 | 7 → 15 → 30 天递进 |
| 连续第 2 次已掌握 | 60 天 |
| 连续第 3 次起已掌握 | 90 天 |

「✅ 会了」= 掌握度升一级（未掌握 → 部分掌握 → 已掌握）；「❌ 不会」= 重置为未掌握、1 天后复习。
也可以手动编辑：切到编辑模式，修改顶部 `mastery`、`next-review`、`review-interval`、`review-count`、`mastery-streak`、`review-log` 等字段。

---

## 三、文件结构

```
错题本/              ← 所有错题笔记存放处
  第18题.md
  P15-3.md
  ...
试卷/
  随机组卷.md        ← 组卷工具
模板/
  错题模板.md        ← 录入错题的模板
脚本/
  规范词表.json      ← 考点/错因词库（录入时自动维护）
  update-mastery.js  ← 更新掌握程度脚本
  迁移统一考点错因.js ← 同义词迁移工具（--dry-run / 自动备份）
  别名映射.json      ← 迁移时的同义词映射配置
  清理孤儿图片.js    ← 清理未被引用的截图（--move 移入回收站）
错题仪表盘.md        ← 统计面板
```

---

## 四、给新设备配置（从零搭建）

如果换电脑或发给别人，Vault 里没有 `.obsidian` 配置时，按以下步骤配置：

### 4.1 安装插件

设置 → 第三方插件 → 关闭安全模式 → 浏览 → 搜索安装：
- **Dataview**
- **Templater**
- **QuickAdd**

### 4.2 配置 Templater

设置 → Templater → **Template folder location** → 填 `模板`

### 4.3 配置 QuickAdd（录入错题）

1. QuickAdd 设置 → 在 **Choice & packages** 区域，点击 **Add Choice** 左边的下拉栏 → 选 **Macro**
2. 下拉栏左边出现输入框 → 输入 `添加错题` → 点 **Add Choice**
3. 新建的 Macro 出现在列表顶端 → 点击右边的 ⚙️ 齿轮
4. 弹窗上方点击 **Template** 标签 → 再点击新出现的 **Template 设置按钮**
5. 依次设置以下选项：

| 选项 | 值 |
|---|---|
| Template Path | 选 `模板/错题模板.md` |
| Create in folder | ✅ 勾选 → 填 `错题本` |
| File Name Format | `{{value:输入题号}}` |
| Open the created file | ✅ 勾选 |

6. 点左上角 ← 返回 QuickAdd 主页
7. 确保 ⚡ 图标点亮

### 4.4 配置「更新掌握程度」（可选）

1. QuickAdd 设置 → **Add Choice** 左侧下拉栏 → 选 **User Script** → 输入 `更新掌握程度` → **Add Choice**
2. 点击该 Choice 的 ⚙️ → **Script Path** 填 `脚本/update-mastery.js` → 返回主页点亮 ⚡

### 4.5 绑定快捷键

设置 → 快捷键 → 搜索 `QuickAdd: 添加错题` → 绑定 `Ctrl+Shift+E`；搜索 `QuickAdd: 更新掌握程度` → 绑定 `Ctrl+Shift+M`

---

## 五、常见问题

**Q: 组卷时截图没显示？**
A: 切换到阅读模式（`Ctrl+E`），Dataview 只在阅读模式渲染。

**Q: 仪表盘数据显示为空？**
A: 先录几道错题，确保笔记 type 字段为 `错题`。

**Q: 如何添加新考点？**
A: 录入时在考点列表里选「✨ 新建考点」，输入新词即可；新词会自动写进 `脚本/规范词表.json`，之后就一直出现在建议列表里。

**Q: 以前用过的同义词（如「无穷小」和「无穷小量」）混在一起了怎么办？**
A: 录入时统一从建议列表选择就不会再产生新变体。历史数据如需合并，在 `脚本/别名映射.json` 里配置同义词映射（如 `{"无穷小": "无穷小量"}`），然后运行 `脚本/迁移统一考点错因.js`，或手动把笔记里的词改成规范写法。脚本支持 `--dry-run`（只预览不写入），写入前会自动把原文件备份到 `脚本/迁移备份/`。

**Q: 旧错题没有 next-review，会出现在今日待复习里吗？**
A: 会。没有设置复习日期的旧题按“已到期”处理，复习一次后就会自动安排下次日期。

**Q: 组卷会连续抽到同一道题吗？**
A: 防重复（`skip-recent-days`，默认 2 天）只作用于**未到期题**；已到期题永远优先出现、按「最久没做过」轮换——所有到期题过一遍之前不会重复。标记「不会」的题 1 天后到期，届时会正常出现在卷子里（与「1 天后复习」的承诺一致）。

**Q: 组卷页点「会了 / 不会」会发生什么？**
A: 「会了」= 掌握度升一级并递进下次复习间隔；「不会」= 重置为未掌握、1 天后复习。两者都会让复习次数 +1，并把这次标记记入 `review-log`（仪表盘「最近复习动态」可见）。

**Q: 错题原图里的截图怎么清理？**
A: 运行 `node 脚本/清理孤儿图片.js` 会先列出所有没有被笔记引用的图片（默认只预览不动文件）；确认后加 `--move` 参数，孤儿图片会被移入 `脚本/图片回收站/<时间戳>/`，再手动删除该文件夹即可。

**Q: 如何备份？**
A: 复制整个项目文件夹即可，`.obsidian` 目录包含所有配置。也可用 Obsidian Sync 或 Git。

---

> 最后更新：2026-08-14
