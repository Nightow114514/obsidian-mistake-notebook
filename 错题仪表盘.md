---
cssclass: error-dashboard
---

# 📊 错题仪表盘

## ⚡ 快捷操作

```dataviewjs
// 按命令名触发 QuickAdd（不依赖命令 ID，手机端手动重建 Choice 也兼容）
const runQuickAdd = (cmdName) => {
  const cmd = (dv.app.commands.listCommands() || []).find((c) => c.name === cmdName);
  if (cmd) dv.app.commands.executeCommandById(cmd.id);
  else new Notice("⚠️ 未找到命令「" + cmdName + "」，请确认 QuickAdd 插件已启用");
};

const row = dv.container.createEl("div");
row.style.marginBottom = "4px";

const btnAdd = row.createEl("button", { text: "✍️ 录入新错题" });
btnAdd.style.padding = "10px 18px";
btnAdd.addEventListener("click", () => {
  // 等价于快捷键 Ctrl+Shift+E
  runQuickAdd("QuickAdd: 添加错题");
});

const btnPaper = row.createEl("button", { text: "🎲 打开随机组卷" });
btnPaper.style.padding = "10px 18px";
btnPaper.style.marginLeft = "8px";
btnPaper.addEventListener("click", () => {
  dv.app.workspace.openLinkText("试卷/随机组卷.md", "", false);
});

const hint = row.createEl("div");
hint.style.marginTop = "6px";
hint.style.fontSize = "0.85em";
hint.style.color = "var(--text-muted)";
hint.textContent = "手机端没有快捷键：点「✍️ 录入新错题」即可录入（等价 Ctrl+Shift+E）；标记掌握程度请打开错题笔记，点笔记底部的「📝 标记掌握程度」按钮。";
```

## 🗓️ 今日待复习

```dataviewjs
const today = dv.date("today");
const toDate = (v) => {
  if (v == null) return null;
  try {
    // Dataview 已把 YAML 日期解析为 Luxon DateTime，这里兼容字符串/Date 的兜底
    if (typeof v === "object" && typeof v.ts === "number" && typeof v.diff === "function") return v;
    return dv.date(String(v));
  } catch (e) { return null; }
};
const fmtDate = (d) => (d && typeof d.toISODate === "function") ? d.toISODate() : (d ? String(d).slice(0, 10) : "尽快");

const pages = Array.from(dv.pages('"错题本"').where(p => p.type === "错题"));
const due = pages.filter(p => {
  const d = toDate(p["next-review"]);
  return !d || d <= today; // 没有复习日期的旧题按「已逾期」处理
});

if (due.length === 0) {
  dv.paragraph("🎉 今日没有到期错题，全部搞定！");
} else {
  const daysLate = (p) => {
    const d = toDate(p["next-review"]);
    if (!d) return null; // 从未安排复习
    return Math.max(0, Math.floor(today.diff(d, "days").days ?? 0));
  };
  const overdue = due
    .filter(p => { const d = toDate(p["next-review"]); return !d || d < today; })
    .sort((a, b) => (daysLate(b) ?? 9999) - (daysLate(a) ?? 9999)); // 逾期越久越靠前
  const dueToday = due
    .filter(p => { const d = toDate(p["next-review"]); return !!d && d >= today; })
    .sort((a, b) => toDate(a["next-review"]).toMillis() - toDate(b["next-review"]).toMillis());

  dv.header(3, `今日待复习：${due.length} 道（⚠️ 逾期 ${overdue.length} · 📌 今日到期 ${dueToday.length}）`);

  const baseCols = (p) => [
    p.file.link,
    p.difficulty || "-",
    (Array.isArray(p["knowledge-point"]) ? p["knowledge-point"].join(" · ") : p["knowledge-point"]) || "-",
    (Array.isArray(p["error-reason"]) ? p["error-reason"].join(" · ") : p["error-reason"]) || "-",
    p["review-count"] || 0
  ];

  if (overdue.length > 0) {
    dv.header(4, "⚠️ 已逾期（优先复习）");
    dv.table(
      ["题目", "难度", "考点", "错因", "逾期", "已复习"],
      overdue.map(p => {
        const late = daysLate(p);
        return [
          ...baseCols(p).slice(0, 4),
          late === null ? "旧题未安排" : `${late} 天`,
          baseCols(p)[4]
        ];
      })
    );
  }
  if (dueToday.length > 0) {
    dv.header(4, "📌 今天到期");
    dv.table(
      ["题目", "难度", "考点", "错因", "已复习", "下次复习"],
      dueToday.map(p => [...baseCols(p), fmtDate(toDate(p["next-review"]))])
    );
  }
}
```

## 按掌握程度统计

```dataview
TABLE 
  length(rows) AS "数量"
FROM "错题本"
WHERE type = "错题"
GROUP BY mastery
```

## 按难度统计

```dataview
TABLE 
  length(rows) AS "数量"
FROM "错题本"
WHERE type = "错题"
GROUP BY difficulty
```

## ⚠️ 薄弱考点 TOP 10（未掌握 + 部分掌握）

```dataview
TABLE 
  length(rows) AS "错题数"
FROM "错题本"
WHERE type = "错题" AND mastery != "已掌握"
FLATTEN knowledge-point AS kp
GROUP BY kp
SORT length(rows) DESC
LIMIT 10
```

## 💡 错因分布（未掌握 + 部分掌握）

```dataview
TABLE 
  length(rows) AS "错题数"
FROM "错题本"
WHERE type = "错题" AND error-reason AND mastery != "已掌握"
FLATTEN error-reason AS reason
GROUP BY reason
SORT length(rows) DESC
```

---

## 📈 掌握程度分布图

```dataviewjs
const pages = dv.pages('"错题本"').where(p => p.type === "错题");
const total = pages.length;
if (total === 0) {
  dv.paragraph("暂无数据，快去录错题吧 ✍️");
} else {
  const counts = { "未掌握": 0, "部分掌握": 0, "已掌握": 0 };
  for (const p of pages) {
    const m = p.mastery || "未掌握";
    if (counts[m] !== undefined) counts[m]++;
  }

  const colors = { "未掌握": "#e74c3c", "部分掌握": "#f39c12", "已掌握": "#27ae60" };

  for (const [label, count] of Object.entries(counts)) {
    const pct = ((count / total) * 100).toFixed(0);
    const bar = "█".repeat(Math.round(count / total * 30));
    dv.paragraph(`**${label}**　${count} 题（${pct}%）`);
    dv.el("div", bar, { style: `color:${colors[label]}; font-size:14px; margin-bottom:8px;` });
  }
}
```

## 📈 每周新增趋势

```dataviewjs
const pages = dv.pages('"错题本"').where(p => p.type === "错题" && p["error-date"]);
if (pages.length === 0) {
  dv.paragraph("暂无数据");
} else {
  // 提取 error-date 的日历日期（与本地时区无关）
  const dateParts = (value) => {
    if (value == null) return null;
    // 模板写入的 date-only 被 js-yaml 解析成 UTC 午夜；手动加引号的字符串被解析为本地午夜。
    // ts 恰好对齐 UTC 午夜 → 用 UTC 分量还原；否则按本地日历取（toISODate）。
    if (typeof value === "object" && typeof value.ts === "number" && !isNaN(value.ts)) {
      if (value.ts % 86400000 === 0) {
        const d = new Date(value.ts);
        return [d.getUTCFullYear(), d.getUTCMonth() + 1, d.getUTCDate()];
      }
      const iso = typeof value.toISODate === "function" ? value.toISODate() : String(value);
      const m = iso.match(/^(\d{4})-(\d{1,2})-(\d{1,2})/);
      return m ? [Number(m[1]), Number(m[2]), Number(m[3])] : null;
    }
    if (value instanceof Date && !isNaN(value.getTime())) {
      return [value.getUTCFullYear(), value.getUTCMonth() + 1, value.getUTCDate()];
    }
    const m = String(value).match(/^(\d{4})-(\d{1,2})-(\d{1,2})/);
    return m ? [Number(m[1]), Number(m[2]), Number(m[3])] : null;
  };
  const pad2 = (n) => String(n).padStart(2, "0");

  // 按周汇总：纯日期运算算周一，避免 toISOString 的 UTC 偏移
  const weeks = {};
  for (const p of pages) {
    const parts = dateParts(p["error-date"]);
    if (!parts) continue;
    const [y, m, d] = parts;
    const dt = new Date(Date.UTC(y, m - 1, d));
    if (isNaN(dt.getTime())) continue;
    const day = dt.getUTCDay() || 7; // 周一=1 … 周日=7
    const monday = new Date(Date.UTC(y, m - 1, d - day + 1));
    const key = `${monday.getUTCFullYear()}-${pad2(monday.getUTCMonth() + 1)}-${pad2(monday.getUTCDate())}`;
    weeks[key] = (weeks[key] || 0) + 1;
  }

  const sorted = Object.entries(weeks).sort();
  const maxCount = Math.max(...sorted.map(([,c]) => c), 1);

  dv.table(
    ["周", "新增", "趋势"],
    sorted.map(([week, count]) => {
      const barLen = Math.round(count / maxCount * 20);
      return [
        week,
        count,
        "▬".repeat(barLen) + " " + count
      ];
    })
  );
}
```

## 📜 最近复习动态

```dataviewjs
const pages = dv.pages('"错题本"').where(p => p.type === "错题" && p["review-log"]);
const entries = [];
for (const p of pages) {
  const logs = Array.isArray(p["review-log"]) ? p["review-log"] : [p["review-log"]];
  for (const raw of logs) {
    const s = String(raw);
    const m = s.match(/^(\d{4}-\d{2}-\d{2})\s+(.+)$/);
    if (m) entries.push({ date: m[1], text: m[2], link: p.file.link });
  }
}
if (entries.length === 0) {
  dv.paragraph("暂无复习记录，复习后这里会出现动态 ✍️");
} else {
  entries.sort((a, b) => b.date.localeCompare(a.date));
  dv.table(
    ["日期", "动态", "题目"],
    entries.slice(0, 10).map((e) => [e.date, e.text, e.link])
  );
}
```
