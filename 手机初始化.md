# 📱 手机初始化（一次性，约 3 分钟）

> 给手机端从零启用插件用。按顺序点一遍即可，不需要传输任何隐藏文件夹。

## 第 0 步：关闭安全模式

设置 → 第三方插件 → 关闭「安全模式 / Restricted mode」开关。

## 第 1 步：一键安装三个插件

点击下面链接会直接跳到对应插件的安装页，点 **Install（安装）** 即可：

- [📦 安装 Dataview](obsidian://show-plugin?id=dataview)
- [📦 安装 Templater](obsidian://show-plugin?id=templater-obsidian)
- [📦 安装 QuickAdd](obsidian://show-plugin?id=quickadd)

## 第 2 步：三个小配置（各 30 秒）

### Templater

设置 → Templater → **Template folder location** → 填 `模板`

### QuickAdd 宏 ①：录入错题（名字必须叫「添加错题」）

1. 设置 → QuickAdd → **Add Choice** 左边下拉栏选 **Macro** → 输入 `添加错题` → 点 **Add Choice**
2. 点它右边的 ⚙️ → 上方切到 **Template** 标签 → 点 **Template 设置**
3. 依次设置：
   - Template Path → 选 `模板/错题模板.md`
   - Create in folder → 勾选，填 `错题本`
   - File Name Format → 填 `{{value:输入题号}}`
   - Open the created file → 勾选
4. 返回 QuickAdd 主页，点亮 ⚡

### QuickAdd 宏 ②：更新掌握程度（名字必须叫「更新掌握程度」）

1. **Add Choice** 下拉栏选 **User Script** → 输入 `更新掌握程度` → 点 **Add Choice**
2. 点 ⚙️ → **Script Path** → 选 `脚本/update-mastery.js` → 返回主页点亮 ⚡

## 第 3 步：附件路径

设置 → 文件与链接 → **附件默认位置** → 选 `错题原图` 子文件夹。

## ✅ 完成自检

打开 `错题仪表盘.md`（阅读模式）：

- 点顶部「✍️ 录入新错题」→ 应弹出题号输入框
- 打开任意错题笔记，点底部「📝 标记掌握程度」→ 应弹出掌握程度选择框

> 按钮是按命令名触发的（QuickAdd: 添加错题 / QuickAdd: 更新掌握程度），只要宏名字起对，手机端新建的配置就能直接工作。
