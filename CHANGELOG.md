# 更新日志

## v2.2.3（2026-08-18）

### 修复

- 录入脚本启动时自动扫描「错题本」全部笔记，把手动添加的考点 / 错因同步进词库（有新增才写盘），下拉建议列表永远包含笔记里已有的全部标签

## v2.2.2（2026-08-18）

### 优化

- 组卷页「会了 / 不会 / 写复盘」改为卷末批量落盘：卷内标记不再立即写盘（从根上避免每次点击触发 Dataview 重渲染导致页面跳顶）；「最后一题标记完成」或点「✅ 本次已做完」时统一写入全部改动（只触发一次刷新）；中途重新进入页面会自动补落盘，防止丢失

## v2.2.1（2026-08-15）

### 变更

- 「我的复盘」区回归简洁占位（移除三小节示例「错在哪 / 正确思路 / 下次注意」），新笔记与历史笔记保持一致；组卷页「📝 写复盘」的记录仍按日期追加到该区
- 迁移脚本重建词表时剔除被别名映射合并掉的旧词（如「等价无穷小公式」合并为「等价无穷小替换」后，旧词不再出现在词表中）

## v2.2.0（2026-08-15）

### 新增

- 组卷页每题新增「📝 写复盘」按钮：内联输入框，保存后按「第 N 次复盘（YYYY-MM-DD）」追加到笔记的「我的复盘」区（无该区时自动创建），题目旁实时显示「已复盘 N 次」
- 每题新增「📖 打开笔记」跳转按钮，点击直达错题本对应笔记
- 标记结果常驻显示：点「会了 / 不会」后，结果与**下次复习日期**持续显示在题目下方（此前刷新后消失，现直接读取笔记最新 `next-review` 字段）

## v2.1.4（2026-08-14）

### 新增

- 组卷页每题新增独立的「👀 查看答案」按钮：可单独展开/收起本题答案；移除全局「显示全部答案」开关；导出 PDF 自动包含全部答案（含单独隐藏的）

## v2.1.3（2026-08-14）

### 修复

- 组卷语义统一与文案修正：「已到期题永远优先、不受防重复限制」现在与 `due-only` 开关无关（此前只在 `due-only: true` 时生效），`skip-recent-days` 防重复窗口仅作用于未到期题
- 修正「本次已做完（2 天内不再抽到）」的错误文案——该承诺只对未到期题成立；按钮旁新增换批规则说明
- 现在「不会 → 1 天后复习」的承诺严格成立：标记次日即到期，届时正常出现在卷子里

## v2.1.2（2026-08-14）

### 修复

- 组卷筛选语义修正：`skip-recent-days` 仅在 `due-only: false`（不限时间）时生效——到期题优先复习，不受「最近抽过」限制。此前仪表盘「今日待复习」里到期的题会被组卷的防重复条件挡住，两边口径不一致
- 到期题多于抽取数时，按「最久没做过」优先抽取（从未做过的最前，其次按 `last-picked` 从旧到新）：逾期题分批轮流覆盖，所有题过一遍之前不会重复，也不会出现「刚做完的题下一批又回来」

## v2.1.1（2026-08-14）

### 修复

- 组卷会话固定：标记「会了 / 不会」不再触发整卷重抽（根因：改写笔记后 Dataview 自动刷新导致重新随机抽题）。当前卷现在持久化在组卷页 frontmatter（`paper-hash` / `paper-questions` / `paper-results`，系统自动维护），刷新、切换笔记、重启 Obsidian 都保持同一套卷，点「✅ 本次已做完」才换新一批；「显示全部答案」开关状态也随之持久化

## v2.1.0（2026-08-14）

### 新增

- 组卷页每题新增「✅ 会了 / ❌ 不会」按钮：点一下即更新掌握程度、复习次数 +1、记入复习轨迹并安排下次复习，自测闭环打通
- 复习轨迹 `review-log`：每次标记掌握程度自动追加一条记录，仪表盘新增「最近复习动态」
- 连续「已掌握」延长复习间隔：第 2 次 60 天、第 3 次起 90 天（新增 `mastery-streak` 字段）
- 组卷页答案「屏幕隐藏 + 打印强制显示」：做题时点「👀 显示全部答案」展开查看；导出 PDF 时答案自动包含、交互按钮自动隐藏
- 孤儿图片清理：新增 `脚本/清理孤儿图片.js`，默认只预览，`--move` 移入 `脚本/图片回收站/`
- 同义词映射改为 `脚本/别名映射.json` 配置文件，无需改代码
- 迁移脚本新增 `--dry-run`（只预览不写入）与 `--no-backup` 开关，默认写入前自动备份到 `脚本/迁移备份/`；支持行内数组写法（`knowledge-point: [a, b]`）；生成的词表与现有词表合并，不再整体覆盖

### 修复

- 仪表盘「每周新增趋势」改为时区无关的周计算，修复跨时区周标签偏移
- 组卷页新增 `show-answer` 开关防自测剧透、显示笔记内全部截图（原来只显示第一张）、点「✅ 本次已做完」后立即重抽新题，无需重新打开笔记
- 录入时在「还要继续添加」环节按 ESC 保留已选内容，不再全部丢弃；正确答案支持留空
- 仪表盘 CSS 兼容新版 Obsidian 的 `.markdown-rendered` 类名；清理 `hotkeys.json` 中无关的 Templater 绑定
- 修复迁移脚本单值字段正则的模板字符串转义错误（`new RegExp` 中 `[^"\\]` 需写 `[^"\\\\]`，此前处理任何含列表字段的数据都会抛错）

### 优化

- 「今日待复习」区分「已逾期」（按逾期天数排序，旧的优先）和「今天到期」两组
- 薄弱考点 TOP 10 与错因分布只统计未掌握 + 部分掌握的题，已掌握题不再干扰薄弱点分析
- 复盘区结构化：错在哪 / 正确思路 / 下次注意

## v2.0.0（2026-08-08）

### 新增

- **智能建议录入**：考点 / 错因改为从词库弹出可打字过滤的建议列表，支持连续添加多个；没有合适词时可选「✨ 新建考点 / 新建错因」
- **规范词表**：新增 `脚本/规范词表.json`，录入时自动维护，新词自动入库，从源头避免同义词分裂
- **数据迁移工具**：新增 `脚本/迁移统一考点错因.js`，可把历史笔记中的同义词统一为规范词

### 变更

- 使用指南与 README 同步更新录入流程说明

## v1.0.0（2026-08-05）

- 仓库化整理：新增 README、.gitignore、.gitattributes
- 移除未使用的 tag-wrangler 插件
- 录入模板支持中途取消
- 随机组卷默认抽题数改为 5
